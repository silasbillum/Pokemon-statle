# Kildekode

Vi anbefaler, at al **kode** (applikation, scripts, tests, der hører til produktet) ligger under **`src/`**, så roden af repoet primært rummer dokumentation, config og værktøjer (fx `Rapport/` til MkDocs).

Oprettelse af undermapper (fx `src/api/`, `src/lib/`) efter behov.
