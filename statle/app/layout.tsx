import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "./components/Navbar";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Statle - A Pokemon stats guessing game",
  description: "A Pokemon stats guessing game inspired by Wordle and Statle. Guess the stats of a random Pokemon and see how you rank against other players!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="min-h-screen bg-gray-100 text-slate-900 transition-colors duration-300 dark:bg-gray-900 dark:text-slate-100">
        <Navbar />
        <main className="flex-grow">{children}</main>
      </body>
    </html>
  );
}
